apiVersion: v1
kind: Service

metadata:
  name: nodejs-app-service

spec:
  type: LoadBalancer

  selector:
    app: nodejs-app

  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP